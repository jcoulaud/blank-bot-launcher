# Followed accounts for the X Filtered Stream rule.
#
# Copy this file to accounts.yaml and replace the handles below with your own
# list. The handles here are illustrative only. They are well-known accounts
# kept as placeholders so the example is concrete, not an endorsement of any
# specific account as a launch target.
#
# The bot installs one rule of the shape:
#   from:handle1 OR from:handle2 OR ...
# X caps the rule at 1024 bytes (about 50 handles in this format).

accounts:
  - handle: elonmusk
  - handle: sama
  - handle: toly
  - handle: cz_binance
  - handle: Pumpfun
