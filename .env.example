# === Required ===
SOLANA_PRIVATE_KEY=                   # base58, hot wallet only; never reuse your main wallet
BLANK_API_KEY=                        # https://blank.build dashboard
BLANK_API_BASE_URL=https://api.blank.build
X_BEARER_TOKEN=                       # X Developer Bearer Token (App-Only)
GOOGLE_GENERATIVE_AI_API_KEY=         # https://aistudio.google.com (free tier)
PINATA_JWT=                           # https://pinata.cloud (free tier)

# === Optional (with defaults) ===
LLM_MODEL=gemini-2.5-flash
IMAGE_MODEL=gemini-2.5-flash-image
CLASSIFIER_THRESHOLD=0.85
MAX_SOL_PER_LAUNCH=0.05
MAX_LAUNCHES_PER_DAY=3
MAX_SOL_PER_DAY=0.15
WARN_IF_BALANCE_ABOVE_SOL=2
RPC_URL=https://api.mainnet-beta.solana.com
ACCOUNTS_FILE=./accounts.yaml
SHUTDOWN_TIMEOUT_S=90
PENDING_LOCK_STALE_S=300              # reprocess queue items locked by a crashed worker after 5 minutes
DB_PATH=./data/bot.db
LOG_LEVEL=info
SKIP_OLDER_THAN_S=300                 # ignore tweets older than 5 minutes on startup
STAKING_SHARE_BPS=8000                # share of supply allocated to staking, in basis points (8000 = 80%)
CIRCUIT_BREAKER_WINDOW_S=900
CIRCUIT_BREAKER_PAUSE_S=60
MAX_CONSECUTIVE_PROVIDER_ERRORS=3
MAX_CONSECUTIVE_IPFS_ERRORS=3
MAX_CONSECUTIVE_LAUNCH_ERRORS=2
MAX_X_API_USD_PER_DAY=25

# === Optional (off by default; uncomment to enable) ===
# DASHBOARD_PORT=3000
